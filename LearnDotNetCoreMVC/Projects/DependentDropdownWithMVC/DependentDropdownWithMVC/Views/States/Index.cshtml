@model IEnumerable<DependentDropdownWithMVC.Models.State>

@{
    ViewData["Title"] = "Index";
}
@* 
<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-light">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                State Code
            </th>
            <th>
                State Name
            </th>
            <th>
                Status
            </th>
            <th>
                Created On
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StateCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StateName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedOn)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StateId" class="btn btn-primary">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StateId" class="btn btn-secondary">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StateId" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table> *@

<div class="card">
    <div class="card-header pd-t-20 d-sm-flex align-items-start justify-content-between bd-b-0 pd-b-0">
        <div>
            @* <h6 class="mg-b-5">State List</h6> *@
            <a asp-action="Create" class="btn btn-light">Create New State</a>
        </div>
    </div><div class="card-body pd-y-30">
        <div class="table-responsive">
            <table class="table table-dashboard mg-b-0">
                <thead>
                    <tr>
                        <th class="tx-uppercase tx-medium tx-10 tx-spacing-1 tx-color-03">State Code</th>
                        <th class="tx-uppercase tx-medium tx-10 tx-spacing-1 tx-color-03">State Name</th>
                        <th class="tx-uppercase tx-medium tx-10 tx-spacing-1 tx-color-03">Status</th>
                        <th class="tx-uppercase tx-medium tx-10 tx-spacing-1 tx-color-03">Created On</th>
                        <th class="text-right">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="tx-medium">@Html.DisplayFor(modelItem => item.StateCode)</td>
                            <td class="tx-color-02">@Html.DisplayFor(modelItem => item.StateName)</td>
                            <td>
                                <span class="badge badge-light">@Html.DisplayFor(modelItem => item.Status)</span>
                            </td>
                            <td class="tx-color-03">@Html.DisplayFor(modelItem => item.CreatedOn)</td>
                            <td class="text-right">
                                <div class="btn-group" role="group">
                                    <a asp-action="Edit" asp-route-id="@item.StateId" class="btn btn-xs btn-white">Edit</a>
                                    <a asp-action="Details" asp-route-id="@item.StateId" class="btn btn-xs btn-white">Details</a>
                                    <a asp-action="Delete" asp-route-id="@item.StateId" class="btn btn-xs btn-white text-danger">Delete</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
